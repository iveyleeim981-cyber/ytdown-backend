services:
  - type: web
    name: ytdown-backend
    runtime: python
    buildCommand: pip install -r requirements.txt && apt-get install -y nodejs || true
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
